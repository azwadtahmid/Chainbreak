# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/
.vscode/

# Never commit these, even though this project has none today
.env
.env.*
*.pem
*.key
*.p12
id_rsa*
secrets*.json
credentials*.json

# Dependencies (this project has none, but keeps a stray install out)
node_modules/
package-lock.json

# Build output
dist/
build/
